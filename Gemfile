source "https://rubygems.org"

# Jekyll static site generator
gem "jekyll", "~> 3.9"

# webrick is required for Ruby 3.x (no longer bundled in stdlib)
gem "webrick", "~> 1.9"

# base64 was removed from Ruby 3.4 stdlib but is still required by safe_yaml (jekyll dep)
gem "base64"
